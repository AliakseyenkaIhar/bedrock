{% extends 'layouts/layout.singular.twig' %}

{% block content %}

	<div class="container">

		<!-- Page-{{ post.ID }}  -->
		<article
			class="{{ post.post_class }} w-full h-auto prose page-contacts"
			id="page-{{ post.ID }}"
		>

			<header class="entry-header page-contacts__header">
				<h1 class="entry-title page-contacts__title">{{ post.title }}</h1>
			</header><!-- .page-contacts__header -->

			<div class="entry-content page-contacts__content">
				{{ post.content|e('wp_kses_post') }}
			</div><!-- .page-contacts__content -->

			<footer class="entry-footer page-contacts__footer">
				{% if post.edit_link %}
					<a href="{{ post.edit_link }}">{{ __('Edit post', 'marusia') }}</a>
				{% endif %}
			</footer><!-- .page-contacts__footer -->

		</article><!-- #post-{{ post.ID }} -->

	</div>
{% endblock %}
