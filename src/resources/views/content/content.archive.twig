{% extends 'layouts/layout.index.twig' %}

{% block content %}
	<div class="container">
		{% for post in posts %}
			{% include 'content/content.single.twig' %}
		{% else %}
			{{ __( 'There are no content yet', 'marusia' ) }}
		{% endfor %}
	</div>
{% endblock %}
